# 🐟 Fish Feeder IoT System - Modular Architecture Requirements
# ============================================================
# Production-ready dependencies for modular Pi Server

# Core Python Libraries
# ---------------------
# Built-in modules used: sys, time, signal, argparse, threading, json, logging
# os, pathlib, typing, dataclasses, enum - No additional installation needed

# Serial Communication (Arduino)
# ------------------------------
pyserial>=3.5
# For Arduino Mega 2560 communication via USB/Serial

# Firebase Integration
# -------------------
firebase-admin>=6.0.0
# Firebase Admin SDK for Realtime Database integration

# Web Framework (Flask API)
# -------------------------
Flask>=2.3.0
Flask-CORS>=4.0.0
# RESTful API server with CORS support for web app integration

# Optional: Production WSGI Server
# --------------------------------
# gunicorn>=21.0.0
# For production deployment (uncomment if needed)

# Optional: Enhanced Logging
# -------------------------
# colorlog>=6.7.0
# For colored console logging (uncomment if needed)

# Development Dependencies (Optional)
# ----------------------------------
# pytest>=7.4.0
# pytest-asyncio>=0.21.0
# For testing modular components (uncomment for development)

# System Requirements
# ------------------
# Python >= 3.8
# Raspberry Pi OS or compatible Linux distribution
# Arduino IDE for firmware upload
# Firebase project with Realtime Database enabled

# Environment Variables Required
# -----------------------------
# ARDUINO_PORT=COM3 (or /dev/ttyUSB0 on Linux)
# ARDUINO_BAUD=115200
# FIREBASE_URL=https://your-project.firebaseio.com/
# FIREBASE_SERVICE_ACCOUNT=path/to/service-account.json (optional)
# WEB_HOST=0.0.0.0
# WEB_PORT=5000
# LOG_LEVEL=INFO 